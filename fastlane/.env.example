# Environment variables for fastlane
# Copy this file to fastlane/.env and fill in your values.
# NEVER commit fastlane/.env (it is ignored by .gitignore).

# --- App identity ---
APP_IDENTIFIER=com.dautov.berlintransportmap

# --- App Store Connect app creation (only needed if the app doesn't exist yet) ---
# APP_STORE_CONNECT_APP_NAME=Berlin Transport Map
# APP_STORE_CONNECT_SKU=berlintransportmap-2026
# APP_STORE_CONNECT_PRIMARY_LOCALE=en-US
# APP_STORE_CONNECT_VERSION=1.0

# Optional (only needed for certain actions)
# APPLE_ID=your@email.com

# Team IDs (recommended for signing + uploads)
# TEAM_ID=27EZNUVV57
# ITC_TEAM_ID=173074305

# --- App Store Connect API Key (recommended) ---
# Option 0: Reuse an existing key JSON from another repo (no copying; good for local dev)
# APP_STORE_CONNECT_API_KEY_JSON_PATH="/absolute/path/to/other/repo/fastlane/api_key/api_key.json"

# Option A (preferred): JSON file with embedded key content.
# Put it at: fastlane/api_key/api_key.json (gitignored)
# Format:
# {
#   "key_id": "...",
#   "issuer_id": "...",
#   "key": "<PASTE_YOUR_PRIVATE_KEY_CONTENT_HERE>",
#   "duration": 1200,
#   "in_house": false
# }

# Optional toggles
# FASTLANE_SKIP_ASSETS=1
