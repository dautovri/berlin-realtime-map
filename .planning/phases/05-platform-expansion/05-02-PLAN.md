---
phase: 05-platform-expansion
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: ["src/components/MapView.js", "src/components/StopMarker.js", "src/components/VehicleMarker.js"]
autonomous: true

must_haves:
  truths:
    - "Map displays transport stops with markers"
    - "Realtime vehicle positions shown on map"
    - "User location tracked and centered"
  artifacts:
    - path: "src/components/MapView.js"
      provides: "Main map component"
      contains: "MapView"
    - path: "src/components/StopMarker.js"
      provides: "Stop markers on map"
      contains: "Marker"
    - path: "src/components/VehicleMarker.js"
      provides: "Vehicle markers on map"
      contains: "Marker"
  key_links:
    - from: "src/components/MapView.js"
      to: "src/api/vbb.js"
      via: "API calls"
      pattern: "getStops|getVehicles"
    - from: "src/components/MapView.js"
      to: "src/components/StopMarker.js"
      via: "render markers"
      pattern: "StopMarker"
---

<objective>
Port core transport features to Android app for feature parity.

Purpose: Ensure Android users can view maps, stops, and realtime vehicles like iOS users.
Output: Functional map component with stops and vehicles display.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-platform-expansion/05-01-PLAN.md
@.planning/phases/01-ui-ux-enhancements/01-02-SUMMARY.md
@.planning/phases/02-smart-features/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Implement map component with react-native-maps</name>
  <files>src/components/MapView.js</files>
  <action>Install react-native-maps. Create MapView component using MapView from react-native-maps. Configure initial region to Berlin. Add map controls and styling matching iOS app design. Handle map region changes and zoom levels.</action>
  <verify>Map renders in Android emulator with Berlin centered, zoom and pan work.</verify>
  <done>Interactive map component displays Berlin region.</done>
</task>

<task type="auto">
  <name>Add transport stops display with markers</name>
  <files>src/components/StopMarker.js, src/components/MapView.js</files>
  <action>Create StopMarker component using Marker from react-native-maps. Fetch stops from VBB API and render markers on map. Add callout with stop name and upcoming departures. Style markers to match iOS design.</action>
  <verify>Taps on markers show stop info, markers appear at correct coordinates.</verify>
  <done>Transport stops visible on map with interactive markers.</done>
</task>

<task type="auto">
  <name>Implement realtime vehicle tracking</name>
  <files>src/components/VehicleMarker.js, src/components/MapView.js</files>
  <action>Create VehicleMarker component for moving vehicles. Fetch vehicle positions from VBB API and update markers in real-time. Add vehicle type icons and direction indicators. Implement position updates every 30 seconds.</action>
  <verify>Vehicle markers appear and move on map, update positions periodically.</verify>
  <done>Realtime vehicle positions displayed and updating.</done>
</task>

</tasks>

<verification>
Test on Android emulator: Map loads, stops show with markers, vehicles update positions, location services work if granted.
</verification>

<success_criteria>
Android app displays map with stops and realtime vehicles, matching core iOS functionality.
</success_criteria>

<output>
After completion, create .planning/phases/05-platform-expansion/05-02-SUMMARY.md
</output></content>
<parameter name="filePath">.planning/phases/05-platform-expansion/05-02-PLAN.md