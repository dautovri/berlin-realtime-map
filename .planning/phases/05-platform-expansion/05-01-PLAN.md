---
phase: 05-platform-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["package.json", "app.json", "src/api/vbb.js", "App.js", "src/screens/Home.js"]
autonomous: true

must_haves:
  truths:
    - "React Native project initialized with Expo"
    - "VBB API client integrated"
    - "Basic navigation structure implemented"
  artifacts:
    - path: "package.json"
      provides: "React Native dependencies"
      contains: "expo"
    - path: "src/api/vbb.js"
      provides: "VBB API integration"
      exports: ["getStops", "getVehicles"]
    - path: "App.js"
      provides: "App navigation"
      contains: "NavigationContainer"
  key_links:
    - from: "App.js"
      to: "src/screens/Home.js"
      via: "import"
      pattern: "import.*Home"
    - from: "src/screens/Home.js"
      to: "src/api/vbb.js"
      via: "API call"
      pattern: "getStops|getVehicles"
---

<objective>
Set up React Native foundation for cross-platform development (Android and web).

Purpose: Establish shared codebase for Android app and web app to achieve feature parity across platforms.
Output: Functional React Native project with VBB API integration and basic navigation.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phases for context
@.planning/phases/01-ui-ux-enhancements/01-01-SUMMARY.md
@.planning/phases/02-smart-features/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Initialize React Native project with Expo</name>
  <files>package.json, app.json</files>
  <action>Initialize new React Native project using Expo CLI for cross-platform development. Run 'npx create-expo-app@latest BerlinTransportRN --template blank-typescript'. Configure app.json for Android and web targets. Install core dependencies: react-navigation/native, react-navigation/stack, react-native-maps for map functionality.</action>
  <verify>Run 'expo start' and confirm project builds without errors, shows Expo dev menu.</verify>
  <done>React Native project created with Expo, dependencies installed, and basic app launches.</done>
</task>

<task type="auto">
  <name>Integrate VBB API client</name>
  <files>src/api/vbb.js</files>
  <action>Create API client module for VBB transport data. Implement getStops() function to fetch nearby stops, getVehicles() for realtime vehicle positions, getDepartures(stopId) for upcoming departures. Use fetch API with VBB API endpoints (reference TripKit patterns from iOS app). Handle authentication and error responses.</action>
  <verify>Call API functions and verify they return expected JSON structure (stops with coordinates, vehicles with positions).</verify>
  <done>VBB API client functions exported and working with test data.</done>
</task>

<task type="auto">
  <name>Set up basic navigation structure</name>
  <files>App.js, src/screens/Home.js</files>
  <action>Install and configure React Navigation. Create stack navigator in App.js. Create Home screen component with map placeholder. Set up navigation structure matching iOS app: Home -> Map view with stops overlay.</action>
  <verify>App launches and shows Home screen with navigation working (can push/pop screens).</verify>
  <done>Basic navigation implemented with Home screen accessible.</done>
</task>

</tasks>

<verification>
Confirm React Native app builds and runs on simulator/emulator. API calls return valid transport data. Navigation transitions work smoothly.
</verification>

<success_criteria>
React Native project ready for feature development with VBB API integration and navigation foundation.
</success_criteria>

<output>
After completion, create .planning/phases/05-platform-expansion/05-01-SUMMARY.md
</output></content>
<parameter name="filePath">.planning/phases/05-platform-expansion/05-01-PLAN.md