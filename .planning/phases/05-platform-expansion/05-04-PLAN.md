---
phase: 05-platform-expansion
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified: ["src/services/AuthService.js", "src/services/SyncService.js", "firebase.json"]
autonomous: false
user_setup:
  - service: firebase
    why: "Cross-platform account sync and data storage"
    env_vars:
      - name: FIREBASE_API_KEY
        source: "Firebase Console -> Project Settings -> General -> Your apps -> Web app config"
      - name: FIREBASE_AUTH_DOMAIN
        source: "Firebase Console -> Project Settings -> General -> Your apps -> Web app config"
      - name: FIREBASE_PROJECT_ID
        source: "Firebase Console -> Project Settings -> General -> Project ID"
    account_setup:
      - task: "Create Firebase project"
        location: "Firebase Console -> Add project"
    dashboard_config:
      - task: "Enable Authentication with Email/Password"
        location: "Firebase Console -> Authentication -> Sign-in method"
      - task: "Enable Firestore database"
        location: "Firebase Console -> Firestore Database -> Create database"

must_haves:
  truths:
    - "User can authenticate across platforms"
    - "Favorites sync between iOS, Android, web"
    - "Journey history shared across devices"
  artifacts:
    - path: "src/services/AuthService.js"
      provides: "Authentication service"
      exports: ["signIn", "signUp", "signOut"]
    - path: "src/services/SyncService.js"
      provides: "Data synchronization"
      exports: ["syncFavorites", "syncHistory"]
    - path: "firebase.json"
      provides: "Firebase configuration"
      contains: "apiKey"
  key_links:
    - from: "src/services/AuthService.js"
      to: "firebase"
      via: "Firebase Auth"
      pattern: "firebase\\.auth"
    - from: "src/services/SyncService.js"
      to: "firebase"
      via: "Firestore"
      pattern: "firebase\\.firestore"
---

<objective>
Implement cross-platform account sync for unified user experience.

Purpose: Allow users to access their data (favorites, history) across iOS, Android, and web platforms.
Output: Firebase-backed authentication and data synchronization.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-platform-expansion/05-02-PLAN.md
@.planning/phases/05-platform-expansion/05-03-PLAN.md
@.planning/phases/02-smart-features/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Set up Firebase project and configuration</name>
  <files>firebase.json, src/services/AuthService.js</files>
  <action>Install Firebase SDK. Create firebase.json with project configuration. Initialize Firebase app with config from environment variables. Set up authentication and Firestore instances.</action>
  <verify>Firebase console shows project connected, no initialization errors in app.</verify>
  <done>Firebase configured and initialized in React Native app.</done>
</task>

<task type="auto">
  <name>Implement user authentication</name>
  <files>src/services/AuthService.js</files>
  <action>Create AuthService with signUp, signIn, signOut functions using Firebase Auth. Handle email/password authentication. Add user state management and persistence across app restarts.</action>
  <verify>User can register new account, login with existing, logout successfully.</verify>
  <done>Authentication service functional with email/password.</done>
</task>

<task type="auto">
  <name>Implement data synchronization</name>
  <files>src/services/SyncService.js</files>
  <action>Create SyncService for favorites and journey history. Use Firestore to store/retrieve user data. Implement sync on login and periodic updates. Handle offline queueing and conflict resolution.</action>
  <verify>Favorites added on one platform appear on others, history syncs across devices.</verify>
  <done>Cross-platform data sync working for favorites and history.</done>
</task>

</tasks>

<verification>
Test sync: Create account on web, add favorite, check appears on Android app. Verify offline behavior.
</verification>

<success_criteria>
User accounts and data synchronize seamlessly across iOS, Android, and web platforms.
</success_criteria>

<output>
After completion, create .planning/phases/05-platform-expansion/05-04-SUMMARY.md
</output></content>
<parameter name="filePath">.planning/phases/05-platform-expansion/05-04-PLAN.md