---
phase: 05-platform-expansion
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: ["src/components/WebMapView.js", "src/components/WebStopMarker.js", "src/components/WebVehicleMarker.js"]
autonomous: true

must_haves:
  truths:
    - "Web app displays interactive map"
    - "Transport stops and vehicles shown on web"
    - "App responsive across screen sizes"
  artifacts:
    - path: "src/components/WebMapView.js"
      provides: "Web-specific map component"
      contains: "MapView"
    - path: "src/components/WebStopMarker.js"
      provides: "Web stop markers"
      contains: "Marker"
    - path: "src/components/WebVehicleMarker.js"
      provides: "Web vehicle markers"
      contains: "Marker"
  key_links:
    - from: "src/components/WebMapView.js"
      to: "src/api/vbb.js"
      via: "API calls"
      pattern: "getStops|getVehicles"
    - from: "src/components/WebMapView.js"
      to: "src/components/WebStopMarker.js"
      via: "render markers"
      pattern: "WebStopMarker"
---

<objective>
Port core transport features to web app for browser access.

Purpose: Enable users to access Berlin Transport Map via web browsers with same core features.
Output: Functional web app with map, stops, and vehicles display.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-platform-expansion/05-01-PLAN.md
@.planning/phases/01-ui-ux-enhancements/01-02-SUMMARY.md
@.planning/phases/02-smart-features/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Configure React Native Web</name>
  <files>package.json, app.json</files>
  <action>Install react-native-web and configure Expo for web target. Update app.json to enable web platform. Set up web-specific entry point and polyfills for browser compatibility.</action>
  <verify>Run 'expo start --web' and confirm app loads in browser without errors.</verify>
  <done>React Native app configured to run on web platform.</done>
</task>

<task type="auto">
  <name>Implement web map component</name>
  <files>src/components/WebMapView.js</files>
  <action>Create WebMapView component using react-native-maps with web support. Configure for browser rendering with Mapbox or Google Maps provider. Set initial Berlin region and handle web-specific map controls.</action>
  <verify>Web app displays map in browser, centered on Berlin with zoom/pan working.</verify>
  <done>Interactive web map component renders correctly.</done>
</task>

<task type="auto">
  <name>Add web transport features</name>
  <files>src/components/WebStopMarker.js, src/components/WebVehicleMarker.js, src/components/WebMapView.js</files>
  <action>Create web-specific marker components for stops and vehicles. Integrate with VBB API for stops and realtime vehicles. Add responsive styling and touch/hover interactions for web.</action>
  <verify>Stops and vehicles appear on web map, markers interactive, responsive on different screen sizes.</verify>
  <done>Web app shows transport stops and realtime vehicles.</done>
</task>

</tasks>

<verification>
Test in multiple browsers: Map loads, stops/vehicles display, responsive on mobile/desktop, no console errors.
</verification>

<success_criteria>
Web app provides core transport features via browser with responsive design.
</success_criteria>

<output>
After completion, create .planning/phases/05-platform-expansion/05-03-SUMMARY.md
</output></content>
<parameter name="filePath">.planning/phases/05-platform-expansion/05-03-PLAN.md