---
phase: 02-smart-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [BerlinTransportMap/RoutePlannerView.swift, BerlinTransportMap/TransportMapView.swift, BerlinTransportMap/RouteService.swift]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can select start and end stops for route planning"
    - "User can choose transport mode for routing"
    - "User sees planned route displayed on map"
  artifacts:
    - path: "BerlinTransportMap/RoutePlannerView.swift"
      provides: "Route planning UI with stop selection and mode picker"
      min_lines: 50
    - path: "BerlinTransportMap/RouteService.swift"
      provides: "Routing API integration with VBB"
      exports: ["planRoute(start:end:mode:)"]
    - path: "BerlinTransportMap/TransportMapView.swift"
      provides: "Route overlay on map"
      contains: "MapPolyline"
  key_links:
    - from: "RoutePlannerView"
      to: "RouteService.planRoute"
      via: "Button action"
      pattern: "planRoute\\("
    - from: "RouteService"
      to: "VBB API"
      via: "TripKit routing"
      pattern: "TripKit.*route"
    - from: "TransportMapView"
      to: "route data"
      via: "state binding"
      pattern: "route.*MapPolyline"
---

<objective>
Implement route planning between stops with transport mode selection

Purpose: Enable users to plan efficient journeys by selecting start/end points and preferred transport modes, integrating with VBB routing APIs.

Output: Functional route planner UI and backend integration displaying routes on the map.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ui-ux-enhancements/01-01-SUMMARY.md
@.planning/phases/01-ui-ux-enhancements/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create route planner UI with stop selection</name>
  <files>BerlinTransportMap/RoutePlannerView.swift</files>
  <action>Create SwiftUI view with TextField for start/end stops, Picker for transport modes (Bus, Train, etc.), and Plan Route button. Use @State for inputs, validate stop names exist in VBB data.</action>
  <verify>Build succeeds, view renders with inputs and button</verify>
  <done>UI displays start/end fields, mode picker, and plan button</done>
</task>

<task type="auto">
  <name>Integrate VBB routing API</name>
  <files>BerlinTransportMap/RouteService.swift</files>
  <action>Create RouteService class using TripKit library to call VBB routing API. Implement planRoute(start:end:mode:) returning route data with coordinates for map overlay.</action>
  <verify>API call succeeds for valid Berlin stops, returns route coordinates</verify>
  <done>Routing service integrates with VBB API and returns route data</done>
</task>

<task type="auto">
  <name>Display route on map</name>
  <files>BerlinTransportMap/TransportMapView.swift</files>
  <action>Add @State for route data, integrate RoutePlannerView as sheet, add MapPolyline for route display using route coordinates from RouteService.</action>
  <verify>Route appears as overlay on map after planning</verify>
  <done>User sees planned route displayed on the map</done>
</task>

</tasks>

<verification>
Test route planning: Select two Berlin stops, choose mode, tap plan - route displays on map.
</verification>

<success_criteria>
User can select start/end stops and transport mode to plan routes, with routes displayed on map.
</success_criteria>

<output>
After completion, create .planning/phases/02-smart-features/02-01-SUMMARY.md
</output></content>
<parameter name="filePath">.planning/phases/02-smart-features/02-01-PLAN.md