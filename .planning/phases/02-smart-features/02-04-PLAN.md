---
phase: 02-smart-features
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [BerlinTransportMap/Models/Journey.swift, BerlinTransportMap/JourneyService.swift, BerlinTransportMap/HistoryView.swift]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can view journey history"
    - "User sees frequent route suggestions"
  artifacts:
    - path: "BerlinTransportMap/Models/Journey.swift"
      provides: "Journey data model"
      contains: "struct Journey"
    - path: "BerlinTransportMap/JourneyService.swift"
      provides: "Journey tracking and history"
      exports: ["saveJourney", "getHistory", "getFrequentRoutes"]
    - path: "BerlinTransportMap/HistoryView.swift"
      provides: "History UI with suggestions"
      min_lines: 40
  key_links:
    - from: "JourneyService"
      to: "Journey model"
      via: "storage"
      pattern: "saveJourney"
    - from: "HistoryView"
      to: "JourneyService.getHistory"
      via: "List"
      pattern: "getHistory"
    - from: "HistoryView"
      to: "JourneyService.getFrequentRoutes"
      via: "suggestions section"
      pattern: "getFrequentRoutes"
---

<objective>
Add journey history and frequent route suggestions

Purpose: Track user journeys and provide insights into travel patterns for better planning.

Output: Journey tracking, history storage, and UI for viewing history with suggestions.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ui-ux-enhancements/01-01-SUMMARY.md
@.planning/phases/01-ui-ux-enhancements/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create journey data model</name>
  <files>BerlinTransportMap/Models/Journey.swift</files>
  <action>Create struct Journey with properties: id, startStop, endStop, transportMode, startTime, endTime, duration. Make Codable for storage.</action>
  <verify>Journey struct compiles and can be encoded/decoded</verify>
  <done>Journey model defined with tracking properties</done>
</task>

<task type="auto">
  <name>Implement journey service</name>
  <files>BerlinTransportMap/JourneyService.swift</files>
  <action>Create JourneyService with UserDefaults storage, implement saveJourney(journey:), getHistory() -> [Journey], getFrequentRoutes() -> [RouteSuggestion] based on frequency.</action>
  <verify>Service saves journeys and returns history and suggestions</verify>
  <done>Journey tracking and analysis implemented</done>
</task>

<task type="auto">
  <name>Add history UI</name>
  <files>BerlinTransportMap/HistoryView.swift</files>
  <action>Create List view for journey history, add section for frequent route suggestions, integrate into app navigation.</action>
  <verify>History list displays journeys, suggestions show frequent routes</verify>
  <done>User can view journey history and frequent suggestions</done>
</task>

</tasks>

<verification>
Complete a journey, view in history, see frequent routes suggested.
</verification>

<success_criteria>
User can view journey history and receive frequent route suggestions.
</success_criteria>

<output>
After completion, create .planning/phases/02-smart-features/02-04-SUMMARY.md
</output></content>
<parameter name="filePath">.planning/phases/02-smart-features/02-04-PLAN.md