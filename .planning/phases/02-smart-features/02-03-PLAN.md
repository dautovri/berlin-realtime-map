---
phase: 02-smart-features
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [BerlinTransportMap/Models/HistoricalData.swift, BerlinTransportMap/PredictionService.swift, BerlinTransportMap/TransportMapView.swift]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User sees predicted arrival times for vehicles"
    - "Predictions are based on historical patterns"
  artifacts:
    - path: "BerlinTransportMap/Models/HistoricalData.swift"
      provides: "Historical arrival data storage"
      contains: "struct HistoricalData"
    - path: "BerlinTransportMap/PredictionService.swift"
      provides: "Prediction algorithm using historical data"
      exports: ["predictArrival(vehicle:stop:)"]
    - path: "BerlinTransportMap/TransportMapView.swift"
      provides: "Display predictions in stop details"
      contains: "predicted arrival"
  key_links:
    - from: "PredictionService"
      to: "HistoricalData"
      via: "historical lookup"
      pattern: "historicalData"
    - from: "TransportMapView"
      to: "PredictionService.predictArrival"
      via: "stop detail view"
      pattern: "predictArrival"
---

<objective>
Include predictive arrival times based on historical data

Purpose: Provide users with estimated arrival times using historical patterns to improve journey planning accuracy.

Output: Historical data model, prediction algorithm, and UI display of predictions.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ui-ux-enhancements/01-01-SUMMARY.md
@.planning/phases/01-ui-ux-enhancements/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create historical data model</name>
  <files>BerlinTransportMap/Models/HistoricalData.swift</files>
  <action>Create struct HistoricalData with properties: stopId, vehicleId, dayOfWeek, timeOfDay, actualArrivalTime. Implement storage using UserDefaults or simple file.</action>
  <verify>Can store and retrieve historical data entries</verify>
  <done>Historical data structure defined and storable</done>
</task>

<task type="auto">
  <name>Implement prediction service</name>
  <files>BerlinTransportMap/PredictionService.swift</files>
  <action>Create PredictionService that loads historical data, implements predictArrival(vehicle:stop:) using average delay for similar conditions (day, time).</action>
  <verify>Prediction returns reasonable time estimates</verify>
  <done>Prediction algorithm calculates estimated arrivals</done>
</task>

<task type="auto">
  <name>Display predictions in UI</name>
  <files>BerlinTransportMap/TransportMapView.swift</files>
  <action>Add prediction display to stop annotation details or popup, show predicted vs realtime arrival times when available.</action>
  <verify>Predictions appear alongside realtime data</verify>
  <done>User sees predictive arrival times in the app</done>
</task>

</tasks>

<verification>
View stop details with historical data - predictions display alongside realtime arrivals.
</verification>

<success_criteria>
User sees predictive arrival times based on historical patterns on map and details.
</success_criteria>

<output>
After completion, create .planning/phases/02-smart-features/02-03-SUMMARY.md
</output></content>
<parameter name="filePath">.planning/phases/02-smart-features/02-03-PLAN.md