---
phase: 02-smart-features
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [BerlinTransportMap/Models/Favorite.swift, BerlinTransportMap/FavoritesService.swift, BerlinTransportMap/FavoritesView.swift, BerlinTransportMap/TransportMapView.swift]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can save stops as favorites"
    - "User can save routes as favorites"
    - "User can view and access saved favorites"
  artifacts:
    - path: "BerlinTransportMap/Models/Favorite.swift"
      provides: "Core Data model for favorites"
      contains: "@Model class Favorite"
    - path: "BerlinTransportMap/FavoritesService.swift"
      provides: "CRUD operations for favorites"
      exports: ["saveFavorite", "loadFavorites"]
    - path: "BerlinTransportMap/FavoritesView.swift"
      provides: "UI to list and manage favorites"
      min_lines: 40
  key_links:
    - from: "FavoritesView"
      to: "FavoritesService.loadFavorites"
      via: "onAppear"
      pattern: "loadFavorites\\(\\)"
    - from: "TransportMapView"
      to: "FavoritesService.saveFavorite"
      via: "save button"
      pattern: "saveFavorite"
    - from: "FavoritesService"
      to: "Core Data"
      via: "context.save()"
      pattern: "context\\.save"
---

<objective>
Add favorites system for saved stops and routes

Purpose: Allow users to save frequently used stops and routes for quick access, improving navigation efficiency.

Output: Core Data model, service layer, and UI for managing favorites.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ui-ux-enhancements/01-01-SUMMARY.md
@.planning/phases/01-ui-ux-enhancements/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create Core Data model for favorites</name>
  <files>BerlinTransportMap/Models/Favorite.swift</files>
  <action>Create SwiftData @Model class Favorite with properties: id, name, type (stop/route), stopId (optional), routeData (optional as Codable).</action>
  <verify>Model compiles, can create Favorite instances</verify>
  <done>Favorite model defined with required properties</done>
</task>

<task type="auto">
  <name>Implement favorites service</name>
  <files>BerlinTransportMap/FavoritesService.swift</files>
  <action>Create FavoritesService with SwiftData context, implement saveFavorite(favorite:), loadFavorites() returning [Favorite], deleteFavorite(id:).</action>
  <verify>Service saves and loads favorites correctly</verify>
  <done>Favorites CRUD operations implemented</done>
</task>

<task type="auto">
  <name>Add favorites UI</name>
  <files>BerlinTransportMap/FavoritesView.swift, BerlinTransportMap/TransportMapView.swift</files>
  <action>Create List view showing favorites, add save button to stop annotations and route results, integrate favorites sheet in TransportMapView.</action>
  <verify>Favorites list displays, save button works</verify>
  <done>User can save and view favorites</done>
</task>

</tasks>

<verification>
Save a stop as favorite, view in favorites list, tap to navigate to it.
</verification>

<success_criteria>
User can save and access favorite stops and routes quickly.
</success_criteria>

<output>
After completion, create .planning/phases/02-smart-features/02-02-SUMMARY.md
</output></content>
<parameter name="filePath">.planning/phases/02-smart-features/02-02-PLAN.md