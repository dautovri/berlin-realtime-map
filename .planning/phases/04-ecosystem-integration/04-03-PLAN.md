---
phase: 04-ecosystem-integration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: ["BerlinTransportMap/BikeService.swift", "BerlinTransportMap/RouteService.swift", "BerlinTransportMap/TransportMapView.swift"]
autonomous: true
user_setup:
  - service: call-a-bike
    why: "Bike-sharing data for multi-modal routing"
    env_vars:
      - name: CALLABIKE_API_KEY
        source: "DB Developer Portal -> API keys"
    account_setup: "Register for DB Developer API access at developer.deutschebahn.com"
must_haves:
  truths:
    - "User can see available bikes on the map"
    - "Bike-sharing integrated into multi-modal route planning"
  artifacts:
    - path: "BerlinTransportMap/BikeService.swift"
      provides: "Bike availability fetching"
      exports: ["BikeService"]
    - path: "BerlinTransportMap/RouteService.swift"
      provides: "Bike-inclusive route planning"
      contains: "bikeSegment"
    - path: "BerlinTransportMap/TransportMapView.swift"
      provides: "Bike markers on map"
      contains: "BikeAnnotation"
  key_links:
    - from: "BerlinTransportMap/BikeService.swift"
      to: "https://api.deutschebahn.com/callabike"
      via: "fetchBikes"
      pattern: "deutschebahn.com/callabike"
    - from: "BerlinTransportMap/RouteService.swift"
      to: "BerlinTransportMap/BikeService.swift"
      via: "bike availability"
      pattern: "BikeService.shared"
---

<objective>
Integrate bike-sharing services for multi-modal routing in Berlin.

Purpose: Enable bike + public transport combinations for flexible mobility.

Output: BikeService for availability, bike markers on map, bike-inclusive routing.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/phases/02-smart-features/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Bike-sharing API Integration</name>
  <files>BerlinTransportMap/BikeService.swift</files>
  <action>Create BikeService singleton with async fetchBikes(latitude: Double, longitude: Double, radius: Double) method using Call-a-Bike API. Parse JSON for bike locations, availability, station info. Implement 10-minute caching. Use CALLABIKE_API_KEY for auth. Handle API rate limits and errors.</action>
  <verify>BikeService compiles, fetchBikes returns BikeStation array with valid Berlin bike data.</verify>
  <done>BikeService successfully fetches bike availability from Call-a-Bike API.</done>
</task>

<task type="auto">
  <name>Bike Integration in Routing</name>
  <files>BerlinTransportMap/RouteService.swift, BerlinTransportMap/TransportMapView.swift</files>
  <action>Add bike markers to TransportMapView showing available bikes at stations. Extend RouteService.planRoute to include bike segments: find nearby bikes for start, nearby docks for end, combine with public transport routes. Add "Include Bikes" toggle in route planner.</action>
  <verify>Map shows bike markers with availability, route planning can include bike + transit combinations, toggle controls bike inclusion.</verify>
  <done>Bike-sharing integrated into multi-modal route planning with map display.</done>
</task>

</tasks>

<verification>
Check map displays bike stations with availability numbers. Enable bike routing, plan multi-modal route showing bike + transit segments.
</verification>

<success_criteria>
User can include bike-sharing in multi-modal route planning.
</success_criteria>

<output>
After completion, create .planning/phases/04-ecosystem-integration/04-03-SUMMARY.md
</output></content>
<parameter name="filePath">.planning/phases/04-ecosystem-integration/04-03-PLAN.md