---
phase: 04-ecosystem-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: ["BerlinTransportMap/EventsService.swift", "BerlinTransportMap/TransportMapView.swift", "BerlinTransportMap/RoutePlannerView.swift"]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "User can view event information on the map"
    - "Events show connected transport options and routing"
  artifacts:
    - path: "BerlinTransportMap/EventsService.swift"
      provides: "Events data fetching and parsing"
      exports: ["EventsService"]
    - path: "BerlinTransportMap/TransportMapView.swift"
      provides: "Event markers on map"
      contains: "EventAnnotation"
    - path: "BerlinTransportMap/RoutePlannerView.swift"
      provides: "Transport connections to events"
      contains: "routeToEvent"
  key_links:
    - from: "BerlinTransportMap/EventsService.swift"
      to: "https://www.berlin.de/sen/web/service/maerkte-feste/events/"
      via: "fetchEvents"
      pattern: "berlin.de/events"
    - from: "BerlinTransportMap/RoutePlannerView.swift"
      to: "BerlinTransportMap/RouteService.swift"
      via: "event transport routing"
      pattern: "planRouteToEvent"
---

<objective>
Add event information with transport connections for comprehensive Berlin mobility.

Purpose: Users can discover events and see how to get there using public transport.

Output: EventsService for API integration, event markers on map, transport routing to events.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/phases/02-smart-features/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Events API Integration</name>
  <files>BerlinTransportMap/EventsService.swift</files>
  <action>Create EventsService class with async fetchEvents() method querying Berlin's official events API. Parse JSON response for event name, location, date/time, description. Implement caching for 1 hour. Handle API pagination and error cases. No authentication required for public API.</action>
  <verify>EventsService compiles, fetchEvents returns array of Event structs with valid Berlin event data.</verify>
  <done>EventsService successfully fetches and parses event data from Berlin events API.</done>
</task>

<task type="auto">
  <name>Event Display and Transport Connections</name>
  <files>BerlinTransportMap/TransportMapView.swift, BerlinTransportMap/RoutePlannerView.swift</files>
  <action>Add event markers to TransportMapView using MapAnnotation, showing event icons at event locations. Modify RoutePlannerView to include "Route to Event" functionality: when event selected, auto-populate destination and plan route. Add event details sheet showing transport connections.</action>
  <verify>Map shows event markers, tapping event opens details with route planning option, route displays transport connections to event location.</verify>
  <done>Events display on map with connected transport options and routing.</done>
</task>

</tasks>

<verification>
Launch app, verify events appear as markers on Berlin map. Select event, check route planning shows transport connections to event location.
</verification>

<success_criteria>
User can view event information with connected transport options.
</success_criteria>

<output>
After completion, create .planning/phases/04-ecosystem-integration/04-02-SUMMARY.md
</output></content>
<parameter name="filePath">.planning/phases/04-ecosystem-integration/04-02-PLAN.md