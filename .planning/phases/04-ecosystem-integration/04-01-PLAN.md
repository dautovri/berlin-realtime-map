---
phase: 04-ecosystem-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["BerlinTransportMap/WeatherService.swift", "BerlinTransportMap/TransportMapView.swift", "BerlinTransportMap/RouteService.swift"]
autonomous: true
user_setup:
  - service: openweathermap
    why: "Weather data for transport recommendations"
    env_vars:
      - name: OPENWEATHER_API_KEY
        source: "OpenWeatherMap Dashboard -> API keys"
    account_setup: "Create free account at openweathermap.org"
must_haves:
  truths:
    - "User sees current weather conditions influencing transport recommendations"
    - "Weather data displays on the map interface"
  artifacts:
    - path: "BerlinTransportMap/WeatherService.swift"
      provides: "Weather data fetching and caching"
      exports: ["WeatherService"]
    - path: "BerlinTransportMap/TransportMapView.swift"
      provides: "Weather overlay on map"
      contains: "WeatherOverlay"
    - path: "BerlinTransportMap/RouteService.swift"
      provides: "Weather-influenced route recommendations"
      contains: "weatherFactor"
  key_links:
    - from: "BerlinTransportMap/WeatherService.swift"
      to: "https://api.openweathermap.org"
      via: "fetchWeather"
      pattern: "openweathermap.org"
    - from: "BerlinTransportMap/RouteService.swift"
      to: "BerlinTransportMap/WeatherService.swift"
      via: "weather data"
      pattern: "WeatherService.shared"
---

<objective>
Integrate weather data to influence transport recommendations and display on map.

Purpose: Weather impacts transport choices (walking vs public transport), providing better personalized recommendations.

Output: WeatherService for API integration, weather overlay on map, weather-influenced route planning.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/phases/02-smart-features/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Weather API Integration</name>
  <files>BerlinTransportMap/WeatherService.swift</files>
  <action>Create WeatherService singleton class with async fetchWeather(latitude: Double, longitude: Double) method using OpenWeatherMap API. Parse JSON response for temperature, precipitation probability, and weather condition. Implement 30-minute caching to avoid excessive API calls. Use OPENWEATHER_API_KEY environment variable for authentication. Handle network errors gracefully with fallbacks.</action>
  <verify>WeatherService compiles without errors, fetchWeather returns valid Weather struct with temperature and condition for Berlin coordinates (52.52, 13.405).</verify>
  <done>WeatherService successfully fetches and parses weather data from OpenWeatherMap API with proper caching and error handling.</done>
</task>

<task type="auto">
  <name>Weather Display and Recommendations</name>
  <files>BerlinTransportMap/TransportMapView.swift, BerlinTransportMap/RouteService.swift</files>
  <action>Add weather overlay to TransportMapView showing current temperature and weather icon (sunny/rainy/cloudy). Modify RouteService.planRoute to factor weather conditions: increase walking preference when weather is good (temperature >15°C, no rain), prefer covered transport when raining. Add weather info panel to map UI.</action>
  <verify>Map displays weather icon and temperature, RouteService recommendations change based on weather (walking suggested for good weather, public transport for bad weather).</verify>
  <done>Weather data influences transport recommendations and displays on the map interface.</done>
</task>

</tasks>

<verification>
Run app, check weather overlay appears on map with current Berlin weather. Plan routes and verify recommendations adapt to weather conditions (e.g., walking vs transit preference).
</verification>

<success_criteria>
Weather data influences transport recommendations and displays on the map.
</success_criteria>

<output>
After completion, create .planning/phases/04-ecosystem-integration/04-01-SUMMARY.md
</output></content>
<parameter name="filePath">.planning/phases/04-ecosystem-integration/04-01-PLAN.md