---
phase: 03-performance-reliability
plan: 04
type: execute
wave: 3
depends_on: ["03-03"]
files_modified: ["BerlinTransportMap/NetworkMonitor.swift", "BerlinTransportMap/OfflineModeView.swift", "BerlinTransportMap/TransportMapView.swift"]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Basic transport info accessible without network"
    - "App detects network status automatically"
    - "Offline mode shows cached data with clear indicators"
  artifacts:
    - path: "BerlinTransportMap/NetworkMonitor.swift"
      provides: "Network connectivity detection"
      min_lines: 30
    - path: "BerlinTransportMap/OfflineModeView.swift"
      provides: "Offline UI indicators"
      contains: "offline"
    - path: "BerlinTransportMap/TransportMapView.swift"
      provides: "Offline-aware map display"
      contains: "isOffline"
  key_links:
    - from: "NetworkMonitor"
      to: "TransportMapView"
      via: "connectivity status"
      pattern: "networkMonitor.*isConnected"
    - from: "TransportMapView"
      to: "OfflineModeView"
      via: "offline overlay"
      pattern: "if.*!isConnected"
---

<objective>
Implement offline mode for basic functionality using cached data.

Purpose: Allow users to access essential transport information without internet connection.
Output: Network monitoring and offline UI.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add network connectivity monitoring</name>
  <files>BerlinTransportMap/NetworkMonitor.swift</files>
  <action>Create NetworkMonitor class using NWPathMonitor to detect network connectivity changes. Publish isConnected status using Combine. Handle different network types (WiFi, cellular, none).</action>
  <verify>NetworkMonitor detects connectivity changes accurately.</verify>
  <done>App monitors network status in real-time.</done>
</task>

<task type="auto">
  <name>Task 2: Create offline mode UI overlay</name>
  <files>BerlinTransportMap/OfflineModeView.swift</files>
  <action>Create OfflineModeView showing offline status with cached data indicators. Display last update times, cache expiration warnings, and option to retry connection. Use appropriate styling for offline state.</action>
  <verify>Offline overlay displays correctly when network unavailable.</verify>
  <done>Clear offline status indicators for users.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate offline mode in map view</name>
  <files>BerlinTransportMap/TransportMapView.swift</files>
  <action>Integrate NetworkMonitor into TransportMapView. Show OfflineModeView overlay when offline. Modify data loading to use only cached data when offline. Prevent network requests during offline state.</action>
  <verify>Map shows cached data when offline, no network errors.</verify>
  <done>Basic functionality works offline using cached data.</done>
</task>

</tasks>

<verification>
Offline mode tested by disabling network, basic transport info accessible.
</verification>

<success_criteria>
User can access basic transport info without internet connection.
</success_criteria>

<output>
After completion, create `.planning/phases/03-performance-reliability/03-04-SUMMARY.md`
</output></content>
<parameter name="filePath">.planning/phases/03-performance-reliability/03-04-PLAN.md