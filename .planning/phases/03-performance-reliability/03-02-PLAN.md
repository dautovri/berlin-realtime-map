---
phase: 03-performance-reliability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: ["BerlinTransportMap/LocationManager.swift", "BerlinTransportMap/VehicleRadarService.swift", "BerlinTransportMap/TransportService.swift"]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Battery usage reduced during tracking"
    - "Location updates optimized for efficiency"
    - "Realtime tracking uses minimal power"
  artifacts:
    - path: "BerlinTransportMap/LocationManager.swift"
      provides: "Optimized location updates"
      contains: "desiredAccuracy"
    - path: "BerlinTransportMap/VehicleRadarService.swift"
      provides: "Efficient vehicle tracking"
      contains: "Timer"
    - path: "BerlinTransportMap/TransportService.swift"
      provides: "Battery-conscious data fetching"
      contains: "background"
  key_links:
    - from: "LocationManager"
      to: "TransportMapView"
      via: "location updates"
      pattern: "locationManager.*location"
    - from: "VehicleRadarService"
      to: "TransportService"
      via: "polling interval"
      pattern: "Timer.*scheduledTimer"
---

<objective>
Optimize battery usage during realtime tracking and location services.

Purpose: Ensure efficient power consumption for continuous use during journeys.
Output: Battery-optimized location and tracking services.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Optimize location manager accuracy</name>
  <files>BerlinTransportMap/LocationManager.swift</files>
  <action>Set appropriate desiredAccuracy for location updates based on use case (kCLLocationAccuracyHundredMeters for general tracking, higher for precise navigation). Reduce update frequency when app is backgrounded.</action>
  <verify>Location services work with reduced accuracy settings, battery usage measured lower.</verify>
  <done>Location updates optimized for battery efficiency.</done>
</task>

<task type="auto">
  <name>Task 2: Implement adaptive vehicle polling</name>
  <files>BerlinTransportMap/VehicleRadarService.swift</files>
  <action>Implement adaptive polling intervals: shorter when user is moving/near stops, longer when stationary. Use Timer with dynamic intervals instead of constant polling. Pause updates when app inactive.</action>
  <verify>Vehicle positions update efficiently, polling adapts to user activity.</verify>
  <done>Vehicle tracking uses minimal battery through smart polling.</done>
</task>

<task type="auto">
  <name>Task 3: Add background task optimization</name>
  <files>BerlinTransportMap/TransportService.swift</files>
  <action>Optimize network requests to run in background queues when possible. Batch multiple requests and use URLSession background sessions for non-urgent data. Cancel requests when user navigates away.</action>
  <verify>Network activity doesn't drain battery excessively, background tasks managed properly.</verify>
  <done>Data fetching optimized for battery usage.</done>
</task>

</tasks>

<verification>
Battery usage measured and optimized for realtime tracking scenarios.
</verification>

<success_criteria>
Battery usage reduced during tracking with measurable improvements.
</success_criteria>

<output>
After completion, create `.planning/phases/03-performance-reliability/03-02-SUMMARY.md`
</output></content>
<parameter name="filePath">.planning/phases/03-performance-reliability/03-02-PLAN.md