---
phase: 03-performance-reliability
plan: 03
type: execute
wave: 2
depends_on: []
files_modified: ["BerlinTransportMap/CacheService.swift", "BerlinTransportMap/TransportService.swift", "BerlinTransportMap/TransportMapView.swift"]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "API responses cached for offline access"
    - "Cache provides faster loading than network"
    - "Cached data expires appropriately"
  artifacts:
    - path: "BerlinTransportMap/CacheService.swift"
      provides: "Advanced caching layer"
      min_lines: 50
    - path: "BerlinTransportMap/TransportService.swift"
      provides: "Cache-integrated transport service"
      contains: "CacheService"
    - path: "BerlinTransportMap/TransportMapView.swift"
      provides: "Cache-aware UI"
      contains: "cache"
  key_links:
    - from: "TransportService"
      to: "CacheService"
      via: "cache lookup"
      pattern: "cacheService.*get"
    - from: "CacheService"
      to: "UserDefaults"
      via: "storage"
      pattern: "UserDefaults.*set"
---

<objective>
Implement advanced caching system for better offline experience and faster loading.

Purpose: Enable offline functionality and reduce network dependency.
Output: Cache service with TTL and offline fallback.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CacheService with TTL</name>
  <files>BerlinTransportMap/CacheService.swift</files>
  <action>Create CacheService class using UserDefaults for storage. Implement set/get methods with TTL (time-to-live) for cache expiration. Support different cache keys for stops, departures, vehicles. Include cache invalidation methods.</action>
  <verify>CacheService compiles, stores and retrieves data with expiration.</verify>
  <done>Caching infrastructure with TTL implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate caching in TransportService</name>
  <files>BerlinTransportMap/TransportService.swift</files>
  <action>Modify TransportService to check cache first before network requests. Store successful responses in cache with appropriate TTL. Handle cache misses gracefully, falling back to network. Add cache invalidation on user actions.</action>
  <verify>Transport data loads from cache when available, falls back to network.</verify>
  <done>Transport service uses cache for improved performance.</done>
</task>

<task type="auto">
  <name>Task 3: Add cache status indicators</name>
  <files>BerlinTransportMap/TransportMapView.swift</files>
  <action>Add visual indicators in UI to show when data is from cache vs fresh. Include cache age information and refresh options. Update UI to show cached data during offline mode.</action>
  <verify>UI shows cache status, users can distinguish fresh vs cached data.</verify>
  <done>Users aware of cache usage and can refresh when needed.</done>
</task>

</tasks>

<verification>
Caching system tested with offline scenarios, provides faster loading.
</verification>

<success_criteria>
Advanced caching enables extended offline functionality.
</success_criteria>

<output>
After completion, create `.planning/phases/03-performance-reliability/03-03-SUMMARY.md`
</output></content>
<parameter name="filePath">.planning/phases/03-performance-reliability/03-03-PLAN.md