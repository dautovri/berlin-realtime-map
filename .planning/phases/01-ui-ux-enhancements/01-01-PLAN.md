---
phase: 01-ui-ux-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [BerlinTransportMap/SettingsView.swift, BerlinTransportMap/ContentView.swift]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can toggle between light and dark modes in settings"
    - "Dark mode automatically follows system preference when enabled"
  artifacts:
    - path: "BerlinTransportMap/SettingsView.swift"
      provides: "Settings view with dark mode toggle"
      contains: "Toggle.*Dark Mode"
    - path: "BerlinTransportMap/ContentView.swift"
      provides: "App-wide color scheme application"
      contains: "preferredColorScheme"
  key_links:
    - from: "BerlinTransportMap/SettingsView.swift"
      to: "BerlinTransportMap/ContentView.swift"
      via: "@AppStorage darkMode"
      pattern: "@AppStorage.*darkMode"
---

<objective>
Implement dark mode support with system detection and manual toggle.

Purpose: Allow users to switch to dark mode for better night-time usability and eye comfort.
Output: Working dark mode toggle in settings that applies app-wide.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Requirements: UI-01 - Implement dark mode with automatic system detection
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dark mode toggle to settings view</name>
  <files>BerlinTransportMap/SettingsView.swift</files>
  <action>Add @AppStorage property for darkMode preference, create Toggle view for "Dark Mode" with binding to the storage. Use .preferredColorScheme(.dark) modifier when darkMode is true, else nil for system default.</action>
  <verify>Open settings screen, verify toggle appears and changing it updates the app's color scheme immediately.</verify>
  <done>Settings view shows dark mode toggle that changes app appearance when toggled.</done>
</task>

<task type="auto">
  <name>Task 2: Apply dark mode globally to content view</name>
  <files>BerlinTransportMap/ContentView.swift</files>
  <action>Add @AppStorage("darkMode", defaultValue: false) property, wrap the main view in .preferredColorScheme(darkMode ? .dark : .light) modifier.</action>
  <verify>Launch app with dark mode toggle on/off, verify entire UI respects the setting (backgrounds, text colors).</verify>
  <done>Dark mode setting applies to all views in the app.</done>
</task>

<task type="auto">
  <name>Task 3: Implement automatic system detection</name>
  <files>BerlinTransportMap/SettingsView.swift, BerlinTransportMap/ContentView.swift</files>
  <action>Add @AppStorage for "useSystemTheme" default true, modify color scheme logic to use nil (system) when useSystemTheme true, else use the manual toggle. Add toggle for "Follow System" in settings.</action>
  <verify>Change device theme, app follows; toggle off "Follow System" and manually set light/dark.</verify>
  <done>Dark mode respects system setting by default with manual override option.</done>
</task>

</tasks>

<verification>
Run app on device/simulator, toggle dark mode in settings, verify UI changes immediately. Change system appearance and confirm automatic switching when enabled.
</verification>

<success_criteria>
User can enable/disable dark mode in settings, and it applies to the entire app. When "Follow System" is enabled, app automatically switches based on device theme.
</success_criteria>

<output>
After completion, create .planning/phases/01-ui-ux-enhancements/01-01-SUMMARY.md
</output></content>
<parameter name="filePath">.planning/phases/01-ui-ux-enhancements/01-01-PLAN.md