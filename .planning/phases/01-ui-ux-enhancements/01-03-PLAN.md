---
phase: 01-ui-ux-enhancements
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [BerlinTransportMap/TransportMapView.swift, BerlinTransportMap/SettingsView.swift, BerlinTransportMap/StopAnnotationView.swift]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can navigate app using VoiceOver"
    - "Touch targets meet minimum size requirements"
  artifacts:
    - path: "BerlinTransportMap/TransportMapView.swift"
      provides: "Accessible map interactions"
      contains: "accessibilityLabel"
    - path: "BerlinTransportMap/SettingsView.swift"
      provides: "Accessible settings controls"
      contains: "accessibilityLabel"
  key_links:
    - from: "BerlinTransportMap/StopAnnotationView.swift"
      to: "VoiceOver"
      via: ".accessibilityLabel"
      pattern: "accessibilityLabel"
---

<objective>
Add accessibility features including VoiceOver support and improved touch targets.

Purpose: Ensure app is usable by people with disabilities and meets accessibility standards.
Output: VoiceOver-compatible interface with proper labels and larger touch areas.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Requirements: UI-03 - Add accessibility features (VoiceOver support, larger touch targets)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add accessibility labels to map elements</name>
  <files>BerlinTransportMap/TransportMapView.swift, BerlinTransportMap/StopAnnotationView.swift</files>
  <action>Add .accessibilityLabel() modifiers to map controls, stop markers, and vehicle annotations with descriptive text including stop names, departure times, and vehicle info.</action>
  <verify>Enable VoiceOver, navigate map elements and hear appropriate descriptions.</verify>
  <done>Map elements have VoiceOver labels.</done>
</task>

<task type="auto">
  <name>Task 2: Increase touch target sizes</name>
  <files>BerlinTransportMap/TransportMapView.swift, BerlinTransportMap/SettingsView.swift</files>
  <action>Apply .contentShape modifier with expanded rectangles to buttons and interactive elements, ensuring minimum 44pt touch targets. Add padding where needed.</action>
  <verify>Measure touch areas in Xcode preview, verify minimum 44x44pt for all interactive elements.</verify>
  <done>All touch targets meet minimum size.</done>
</task>

<task type="auto">
  <name>Task 3: Add accessibility traits and hints</name>
  <files>BerlinTransportMap/TransportMapView.swift, BerlinTransportMap/SettingsView.swift</files>
  <action>Add .accessibilityHint() for complex interactions, .accessibilityTraits() for buttons vs static elements, and .accessibilityValue() for dynamic content like departure times.</action>
  <verify>VoiceOver announces correct traits, hints, and values for all elements.</verify>
  <done>Full accessibility implementation complete.</done>
</task>

</tasks>

<verification>
Enable VoiceOver in iOS settings, navigate through app using gestures, verify all elements are readable and controls are operable. Test with Accessibility Inspector.
</verification>

<success_criteria>
App passes basic accessibility audit with VoiceOver working for all key functions and touch targets meeting minimum size requirements.
</success_criteria>

<output>
After completion, create .planning/phases/01-ui-ux-enhancements/01-03-SUMMARY.md
</output></content>
<parameter name="filePath">.planning/phases/01-ui-ux-enhancements/01-03-PLAN.md