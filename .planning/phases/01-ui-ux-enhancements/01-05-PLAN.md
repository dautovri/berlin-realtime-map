---
phase: 01-ui-ux-enhancements
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [BerlinTransportMap/Theme.swift, BerlinTransportMap/SettingsView.swift, BerlinTransportMap/TransportMapView.swift]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can select from multiple color themes"
    - "Selected theme applies to map appearance"
  artifacts:
    - path: "BerlinTransportMap/Theme.swift"
      provides: "Theme definitions and colors"
      contains: "enum Theme"
    - path: "BerlinTransportMap/SettingsView.swift"
      provides: "Theme selector in settings"
      contains: "Picker.*Theme"
  key_links:
    - from: "BerlinTransportMap/SettingsView.swift"
      to: "BerlinTransportMap/TransportMapView.swift"
      via: "@AppStorage theme"
      pattern: "@AppStorage.*theme"
---

<objective>
Implement customizable map themes and styles for personalization.

Purpose: Allow users to personalize the map appearance with different color schemes.
Output: Theme selector in settings that changes map colors and styles.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Requirements: UI-05 - Implement customizable map themes and styles
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define theme model</name>
  <files>BerlinTransportMap/Theme.swift</files>
  <action>Create Theme enum with cases like classic, dark, colorful, minimal. Each theme defines colors for stops, routes, backgrounds, etc.</action>
  <verify>Theme enum compiles with color properties.</verify>
  <done>Theme model exists with color definitions.</done>
</task>

<task type="auto">
  <name>Task 2: Add theme selector to settings</name>
  <files>BerlinTransportMap/SettingsView.swift</files>
  <action>Add @AppStorage for selectedTheme, create Picker with theme options. Display theme previews if possible.</action>
  <verify>Settings show theme picker with available options.</verify>
  <done>Theme selector in settings.</done>
</task>

<task type="auto">
  <name>Task 3: Apply themes to map view</name>
  <files>BerlinTransportMap/TransportMapView.swift, BerlinTransportMap/StopAnnotationView.swift</files>
  <action>Add @AppStorage for theme, apply theme colors to markers, routes, and backgrounds in map and annotation views.</action>
  <verify>Changing theme in settings immediately updates map colors.</verify>
  <done>Themes apply to map appearance.</done>
</task>

</tasks>

<verification>
Select different themes in settings, verify map colors change accordingly. Test with different map styles.
</verification>

<success_criteria>
User can select from multiple themes in settings, and the selected theme changes the colors and styling of map elements.
</success_criteria>

<output>
After completion, create .planning/phases/01-ui-ux-enhancements/01-05-SUMMARY.md
</output></content>
<parameter name="filePath">.planning/phases/01-ui-ux-enhancements/01-05-PLAN.md