---
phase: 01-ui-ux-enhancements
plan: 06
type: execute
wave: 1
depends_on: []
files_modified: [BerlinTransportMap/TransportMapView.swift]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "App builds successfully without syntax errors"
    - "TransportMapView compiles without malformed code structures"
    - "Duplicate sheet closures and task blocks are removed"
  artifacts:
    - path: "BerlinTransportMap/TransportMapView.swift"
      provides: "Fixed syntax and structure"
      min_lines: 1010
  key_links:
    - from: "BerlinTransportMap/TransportMapView.swift"
      to: "build system"
      via: "syntax validation"
      pattern: "struct TransportMapView"
---

<objective>
Fix TransportMapView.swift syntax errors preventing app build and testing of UI/UX enhancements
</objective>

Purpose: Remove duplicate sheet closures and malformed code structures that cause build failures, enabling UAT testing of dark mode and map visualization features
Output: Clean, syntactically correct TransportMapView.swift that compiles successfully

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-ui-ux-enhancements/01-01-SUMMARY.md
@.planning/phases/01-ui-ux-enhancements/01-02-SUMMARY.md
@.planning/STATE.md
@.planning/ROADMAP.md

# Gap closure context
@.planning/phases/01-ui-ux-enhancements/01-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Fix duplicate sheet closures in TransportMapView</name>
  <files>BerlinTransportMap/TransportMapView.swift</files>
  <action>Remove duplicate .sheet(isPresented: $showingAbout) closures - there are two identical sheet declarations around lines 231 and 266. Keep only the first one and remove the duplicate.</action>
  <verify>Search for "sheet.*showingAbout" and confirm only one instance exists</verify>
  <done>Single sheet closure for showingAbout remains</done>
</task>

<task type="auto">
  <name>Fix duplicate task blocks</name>
  <files>BerlinTransportMap/TransportMapView.swift</files>
  <action>Remove duplicate .task { } blocks - there are two identical task declarations around lines 273-278 and 279-284. Keep only the first one and remove the duplicate.</action>
  <verify>Search for ".task" and confirm only one instance exists</verify>
  <done>Single task block remains</done>
</task>

<task type="auto">
  <name>Validate syntax and build</name>
  <files>BerlinTransportMap/TransportMapView.swift</files>
  <action>Run xcodebuild to validate the file compiles without syntax errors</action>
  <verify>xcodebuild clean build succeeds without errors</verify>
  <done>File compiles successfully</done>
</task>

</tasks>

<verification>
App builds successfully in Xcode
</verification>

<success_criteria>
- TransportMapView.swift compiles without syntax errors
- Duplicate sheet closures removed
- Duplicate task blocks removed
- Build succeeds enabling UAT testing
</success_criteria>

<output>
After completion, create .planning/phases/01-ui-ux-enhancements/01-06-SUMMARY.md
</output></content>
<parameter name="filePath">.planning/phases/01-ui-ux-enhancements/01-06-PLAN.md